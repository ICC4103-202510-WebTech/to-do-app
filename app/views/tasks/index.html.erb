<h1>Mis Tareas</h1>
<%= link_to "Nuevo", new_task_path, class: "btn btn-primary" %>
<ul>
    <% @tasks.each do |task| %>
        <li><%= link_to task.name, task_path(task) %> - <%= link_to task.project.name, project_path(task.project) %>
        | <%= link_to "Editar", edit_task_path(task) %>
        | <%= link_to "Eliminar", task_path(task), data: {
            turbo_method: :delete,
            turbo_confirm: "Estás seguro de eliminar la tarea?"
        } %>
        </li>
    <% end %>
</ul>